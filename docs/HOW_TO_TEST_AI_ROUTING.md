# Как вручную проверить AI-routing форвардинг между 3 нодами

## Шаг 0: Запустите визуализацию (Терминал 0) - Опционально

Для визуального отображения отправки сообщений:

```bash
cd /Users/boris/Desktop/meshlink
cargo run --bin viz
```

**Что вы увидите:**
- Граф сети с нодами и связями
- Анимацию при отправке/получении сообщений (волна по линиям)
- Пульсацию нод при событиях
- Статистику и последние события

**Управление:**
- `q` или `Esc` - выход

**Оставьте этот терминал открытым!**

---

## Шаг 1: Запустите первую ноду (Терминал 1)

```bash
cd /Users/boris/Desktop/meshlink
RUST_LOG=info cargo run --bin core --release -- 8082 127.0.0.1:8083 127.0.0.1:8084
```

**Что вы увидите:**
- `Created new node with ID: <uuid>`
- `Listening on: 0.0.0.0:8082`
- `API server listening on 127.0.0.1:17082`
- `Heartbeat - Connected: X/2 peers`

**Оставьте этот терминал открытым!**

---

## Шаг 2: Запустите вторую ноду (Терминал 2)

```bash
cd /Users/boris/Desktop/meshlink
RUST_LOG=info cargo run --bin core --release -- 8083 127.0.0.1:8082 127.0.0.1:8084
```

**Что вы увидите:**
- `Created new node with ID: <uuid>`
- `Listening on: 0.0.0.0:8083`
- `API server listening on 127.0.0.1:17083`
- `Connected to peer 127.0.0.1:8082`
- `Heartbeat - Connected: X/2 peers`

**Оставьте этот терминал открытым!**

---

## Шаг 3: Запустите третью ноду (Терминал 3)

```bash
cd /Users/boris/Desktop/meshlink
RUST_LOG=info cargo run --bin core --release -- 8084 127.0.0.1:8082 127.0.0.1:8083
```

**Что вы увидите:**
- `Created new node with ID: <uuid>`
- `Listening on: 0.0.0.1:8084`
- `API server listening on 127.0.0.1:17084`
- `Connected to peer 127.0.0.1:8082` или `127.0.0.1:8083`
- `Heartbeat - Connected: X/2 peers`

**Оставьте этот терминал открытым!**

---

## Шаг 4: Дождитесь подключений

Подождите 10-15 секунд, пока все ноды подключатся друг к другу.

**Проверьте статус (Терминал 4):**

```bash
# Статус ноды 1
MESHLINK_API_PORT=17082 cargo run --bin cli -- status

# Статус ноды 2
MESHLINK_API_PORT=17083 cargo run --bin cli -- status

# Статус ноды 3
MESHLINK_API_PORT=17084 cargo run --bin cli -- status
```

Вы должны увидеть `Connected: 2` для каждой ноды.

---

## Шаг 5: Проверьте список пиров

```bash
# Пиры ноды 1
MESHLINK_API_PORT=17082 cargo run --bin cli -- peers

# Пиры ноды 2
MESHLINK_API_PORT=17083 cargo run --bin cli -- peers

# Пиры ноды 3
MESHLINK_API_PORT=17084 cargo run --bin cli -- peers
```

Вы должны увидеть по 2 подключенных пира на каждой ноде.

---

## Шаг 6: Отправьте сообщение и проверьте форвардинг

**⚠️ ВАЖНО:** Для проверки форвардинга используйте **broadcast**, а не `send`!

### ❌ Неправильно (send - не форвардится):
```bash
# Сообщение адресовано конкретному пиру, получатель не форвардит его
MESHLINK_API_PORT=17082 cargo run --bin cli -- send <peer_id> "Test message"
```

### ✅ Правильно (broadcast - форвардится):
```bash
# Broadcast сообщение адресовано всем, каждая нода форвардит его другим
MESHLINK_API_PORT=17082 cargo run --bin cli -- broadcast "Broadcast test for AI-routing"
```

**Почему?** 
- `send` создает сообщение с `to: Some(peer_id)` - получатель видит, что сообщение для него, и не форвардит
- `broadcast` создает сообщение с `to: None` - каждая нода форвардит его другим нодам через AI-routing

---

## Шаг 7: Проверьте визуализацию и логи форвардинга

### В визуализации (если запущена):

**При отправке broadcast сообщения вы увидите:**
- 📤 **Волну по линиям** - анимация передачи сообщения от ноды к ноде
- ◉ **Пульсацию нод** - ноды, отправляющие/получающие сообщения, будут пульсировать
- 🔗 **Подсветку связей** - линии между нодами будут подсвечиваться при передаче
- **Статистику** - внизу экрана будет показано последнее событие с иконкой 📤

**Последовательность анимации:**
1. Нода 1 отправляет → волна по линиям к нодам 2 и 3
2. Нода 2 получает и форвардит → волна к ноде 3
3. Нода 3 получает и форвардит → волна к ноде 2

### В логах (терминалы 1, 2 и 3):

1. **При отправке сообщения:**
   ```
   📤 Sending mesh message <id> to N peer(s)
      Content: "ваше сообщение"
   Message <id> queued for sending to <peer_id>
   ```

2. **При получении сообщения:**
   ```
   📨 Received data message <id> from <peer_id>: N bytes
      Content: "ваше сообщение"
   ```

3. **При форвардинге (AI-routing):**
   ```
   🎯 AI-Routing: Forwarding mesh message <id> to N peer(s): [peer1 (score: 0.85), peer2 (score: 0.72), ...]
   Forwarded mesh message <id> to <peer_id> (score: 0.85)
   ```

---

## Что смотреть в логах

### Успешный форвардинг выглядит так:

**Нода 1 (отправитель):**
```
📤 Sending mesh message abc-123 to 2 peer(s)
   Content: "Test message"
Message abc-123 queued for sending to node2-id
Message abc-123 queued for sending to node3-id
```

**Нода 2 (получатель и форвардер):**
```
📨 Received data message abc-123 from node1-id: 241 bytes
   Content: "Test message"
🎯 AI-Routing: Forwarding mesh message abc-123 to 1 peer(s): [node3-id (score: 0.82)]
Forwarded mesh message abc-123 to node3-id (score: 0.82)
```

**Нода 3 (получатель):**
```
📨 Received data message abc-123 from node2-id: 241 bytes
   Content: "Test message"
```

---

## Проверка метрик и scores

Чтобы увидеть метрики пиров, подождите 30-40 секунд после подключения (чтобы прошло несколько ping/pong циклов), затем отправьте сообщение.

В логах вы увидите scores пиров при форвардинге:
- **Score 0.8-1.0** = отличный пир (низкая latency, высокая reliability)
- **Score 0.5-0.8** = хороший пир
- **Score 0.0-0.5** = плохой пир (высокая latency или низкая reliability)

---

## Устранение проблем

### Проблема: Ноды не подключаются
- Убедитесь, что порты свободны: `lsof -ti:8082,8083,8084`
- Убейте старые процессы: `pkill -f "target/.*/core"`

### Проблема: Не видно AI-routing логов
- AI-routing срабатывает только при **форвардинге** (когда нода получает сообщение и пересылает его дальше)
- Для broadcast сообщения: нода 1 отправляет нодам 2 и 3, затем нода 2 форвардит ноде 3 (и наоборот)
- Убедитесь, что используете `RUST_LOG=info` для видимости логов

### Проблема: Нет scores в логах
- Scores появляются только после измерения latency через ping/pong
- Подождите 30-40 секунд после подключения
- Ping отправляется каждые 30 секунд при timeout чтения

---

## Быстрый тест одной командой

```bash
# Очистка
pkill -f "target/.*/core" 2>/dev/null; sleep 1

# Запуск 3 нод в фоне
RUST_LOG=info cargo run --bin core --release -- 8082 127.0.0.1:8083 127.0.0.1:8084 > /tmp/node1.log 2>&1 &
N1=$!
sleep 2
RUST_LOG=info cargo run --bin core --release -- 8083 127.0.0.1:8082 127.0.0.1:8084 > /tmp/node2.log 2>&1 &
N2=$!
sleep 2
RUST_LOG=info cargo run --bin core --release -- 8084 127.0.0.1:8082 127.0.0.1:8083 > /tmp/node3.log 2>&1 &
N3=$!

# Ждем подключений
sleep 15

# Отправляем сообщение
PEER_ID=$(MESHLINK_API_PORT=17082 cargo run --bin cli -- peers 2>&1 | grep -v "warning\|Compiling\|Finished" | grep Connected | head -1 | awk '{print $1}')
MESHLINK_API_PORT=17082 cargo run --bin cli -- send "$PEER_ID" "AI-routing test" 2>&1 | grep -v "warning\|Compiling\|Finished"

sleep 3

# Проверяем логи
echo "=== Node 1 logs ==="
grep -E "(Sending|AI-Routing|Forwarding)" /tmp/node1.log | tail -5
echo ""
echo "=== Node 2 logs ==="
grep -E "(Received|AI-Routing|Forwarding)" /tmp/node2.log | tail -5
echo ""
echo "=== Node 3 logs ==="
grep -E "(Received|AI-Routing|Forwarding)" /tmp/node3.log | tail -5

# Очистка
kill $N1 $N2 $N3 2>/dev/null
```

---

**Готово! Теперь вы можете видеть, как AI-routing выбирает оптимальные маршруты для форвардинга сообщений.**

